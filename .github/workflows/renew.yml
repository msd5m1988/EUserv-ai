name: EuServ Auto Renew

on:
  schedule:
    # 每月 12-18 號執行
    - cron: '30 2 12-18 * *'
  workflow_dispatch: # 👈 確保這行還在，才有手動按鈕

jobs:
  renew:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install playwright imaplib2
          playwright install chromium

      # 測試時先註釋掉 Random Sleep，方便立即看結果
      # - name: Random Sleep
      #   run: sleep $(( (RANDOM % 3600) + 1 ))s

      - name: Run Renew Script
        env:
          EUSERV_EMAIL: ${{ secrets.EUSERV_EMAIL }}
          EUSERV_PASSWORD: ${{ secrets.EUSERV_PASSWORD }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python renew.py

      # 👈 注意：這一步必須跟上面的 "- name: Run Renew Script" 對齊
      - name: Upload Debug Screenshot
        if: always() 
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots
          path: "*.png"
          retention-days: 5
